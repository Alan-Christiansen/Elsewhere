# Elsewhere
Implementation repository for the `ObsPlugin - Elsewhere` project.

An Obsidian plugin for creating and editing `.url` Internet Shortcut files without leaving your vault.

> This README is the internal stub. It is replaced by the user-facing README before release, per decision D-004.
## Canonical project record
Project purpose, lifecycle, decisions, current outcome, blockers, and next action live in the project's human-owned record:

`Internal project record (not published).`

Keep code, tests, runtime assets, and implementation documentation here. Link across the boundary instead of duplicating detailed project state.
